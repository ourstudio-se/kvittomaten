# our-studio

Minimal Next.js + TypeScript app with **shadcn/ui**.

## Prerequisites

- Node.js (modern LTS recommended)

## Getting started

```bash
npm install
npm run dev
```

Open `http://localhost:3000` to see **Hello world**.

## Notes

- shadcn/ui is configured via `components.json` and installed components live in `components/ui/`.
